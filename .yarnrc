# Yarn v1 enforces package.json "engines" on every command, including `yarn run`.
# If your Node patch is below toolchain minimums (e.g. 22.5 vs 22.13), set this so
# scripts like `yarn test:env:down` still run. Prefer Node ^20.19 || ^22.13 || >=24.
--ignore-engines true
